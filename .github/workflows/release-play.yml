name: Release to Play

# Manual, signed Android App Bundle build + (optional) Google Play upload.
# Defaults are deliberately safe: builds the .aab and uploads it as a workflow
# artifact, and only pushes to Play when `publish` is explicitly enabled, to the
# internal track, as a draft.
#
# Required repo secrets (see docs/PLAY_PUBLISHING.md):
#   UPLOAD_KEYSTORE_BASE64     - base64 of the upload keystore (.jks)
#   UPLOAD_KEYSTORE_PASSWORD   - keystore password
#   UPLOAD_KEY_ALIAS           - key alias
#   UPLOAD_KEY_PASSWORD        - key password
#   PLAY_SERVICE_ACCOUNT_JSON  - Google Play service-account JSON (publish only)

on:
  workflow_dispatch:
    inputs:
      track:
        description: "Play track"
        type: choice
        options: [internal, alpha, beta, production]
        default: internal
      status:
        description: "Release status"
        type: choice
        options: [draft, completed]
        default: draft
      publish:
        description: "Publish to Play (off = build + upload the .aab artifact only)"
        type: boolean
        default: false

jobs:
  build-publish:
    name: Build signed AAB${{ inputs.publish && ' + publish' || '' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so versionCode = commit count is correct

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission to Gradle wrapper
        run: chmod +x gradlew

      - name: Decode upload keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.UPLOAD_KEYSTORE_BASE64 }}
        run: echo "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/upload.jks"

      - name: Compute versionCode from commit count
        run: echo "VERSION_BUILD=$(git rev-list --count HEAD)" >> "$GITHUB_ENV"

      - name: Read application id
        run: echo "APP_ID=$(./gradlew -q :app:printApplicationId)" >> "$GITHUB_ENV"

      # Preflight the Google Play Developer API before the upload step, because
      # r0adkll/upload-google-play reports every API failure as a useless
      # "Unknown error occurred." This authenticates with the service account
      # and opens an edit (the same first call the upload makes), surfacing the
      # real HTTP status + Google error message so failures are diagnosable:
      #   401 -> bad/blocked credentials
      #   403 -> service account lacks permission, or the Play Developer API
      #          isn't enabled for its project
      #   404 -> app/package not found, i.e. no manual first release exists yet
      # Self-contained (jq + openssl + curl, all on ubuntu-latest); never prints
      # the private key or access token. Only runs when actually publishing.
      - name: Preflight Google Play API access
        if: ${{ inputs.publish }}
        env:
          SERVICE_ACCOUNT_JSON: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          set -euo pipefail
          if [ -z "${SERVICE_ACCOUNT_JSON:-}" ]; then
            echo "::error::PLAY_SERVICE_ACCOUNT_JSON secret is empty or unset — set it to the full service-account JSON."
            exit 1
          fi
          sa="$RUNNER_TEMP/sa.json"
          printf '%s' "$SERVICE_ACCOUNT_JSON" > "$sa"
          if ! jq -e . "$sa" >/dev/null 2>&1; then
            echo "::error::PLAY_SERVICE_ACCOUNT_JSON is not valid JSON."
            exit 1
          fi
          client_email=$(jq -r '.client_email // empty' "$sa")
          token_uri=$(jq -r '.token_uri // "https://oauth2.googleapis.com/token"' "$sa")
          if [ -z "$client_email" ]; then
            echo "::error::Service-account JSON is missing client_email — the secret is malformed."
            exit 1
          fi
          echo "Service account: $client_email"
          echo "Package:         $APP_ID"

          # Mint an RS256-signed JWT and exchange it for an androidpublisher access token.
          b64url() { openssl base64 -e -A | tr '+/' '-_' | tr -d '='; }
          now=$(date +%s); exp=$((now + 300))
          header_b64=$(printf '%s' '{"alg":"RS256","typ":"JWT"}' | b64url)
          claim_b64=$(jq -nc --arg iss "$client_email" --arg aud "$token_uri" \
            --argjson iat "$now" --argjson exp "$exp" \
            '{iss:$iss, scope:"https://www.googleapis.com/auth/androidpublisher", aud:$aud, iat:$iat, exp:$exp}' \
            | b64url)
          jq -r '.private_key' "$sa" > "$RUNNER_TEMP/key.pem"
          sig=$(printf '%s' "$header_b64.$claim_b64" | openssl dgst -sha256 -sign "$RUNNER_TEMP/key.pem" | b64url)
          jwt="$header_b64.$claim_b64.$sig"

          access_token=$(curl -s -X POST "$token_uri" \
            --data-urlencode "grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer" \
            --data-urlencode "assertion=$jwt" | jq -r '.access_token // empty')
          if [ -z "$access_token" ]; then
            echo "::error::Could not obtain an access token — the service-account credentials were rejected by $token_uri."
            exit 1
          fi

          # Open (then abandon) an edit — exactly the first call the upload makes.
          resp="$RUNNER_TEMP/edit.json"
          http=$(curl -s -o "$resp" -w "%{http_code}" -X POST \
            -H "Authorization: Bearer $access_token" \
            "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/${APP_ID}/edits")
          echo "Play API edits.insert -> HTTP $http"
          echo "Response body:"; cat "$resp"; echo
          if [ "$http" = "200" ] || [ "$http" = "201" ]; then
            edit_id=$(jq -r '.id // empty' "$resp")
            if [ -n "$edit_id" ]; then
              curl -s -o /dev/null -X DELETE \
                -H "Authorization: Bearer $access_token" \
                "https://androidpublisher.googleapis.com/androidpublisher/v3/applications/${APP_ID}/edits/${edit_id}" || true
            fi
            echo "Play API access OK — proceeding to upload."
          else
            echo "::error::Play Developer API rejected the request (HTTP $http). See the response body above for the exact cause (403=permission/API not enabled, 404=app or first release missing, 401=bad credentials)."
            exit 1
          fi

      - name: Build signed release bundle
        env:
          QUICLOC_KEYSTORE_FILE: ${{ runner.temp }}/upload.jks
          QUICLOC_KEYSTORE_PASSWORD: ${{ secrets.UPLOAD_KEYSTORE_PASSWORD }}
          QUICLOC_KEY_ALIAS: ${{ secrets.UPLOAD_KEY_ALIAS }}
          QUICLOC_KEY_PASSWORD: ${{ secrets.UPLOAD_KEY_PASSWORD }}
        run: ./gradlew bundleRelease -PversionBuild=${{ env.VERSION_BUILD }}

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: quicloc-release-aab-${{ env.VERSION_BUILD }}
          path: app/build/outputs/bundle/release/*.aab
          if-no-files-found: error

      - name: Publish to Google Play
        if: ${{ inputs.publish }}
        uses: r0adkll/upload-google-play@v1.1.3
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: ${{ env.APP_ID }}
          releaseFiles: app/build/outputs/bundle/release/*.aab
          track: ${{ inputs.track }}
          status: ${{ inputs.status }}
